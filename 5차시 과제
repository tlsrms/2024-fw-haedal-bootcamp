class Rectangle {
		constructor(height, width) {
				this.height = height;
				this.width = width;
		}
		calArea() {
				console.log(this.height * this.width / 2);
		}
}

const h = prompt("높이를 입력하세요.");
const w = prompt("너비를 입력하세요.");
const answer = new Rectangle(h, w);
answer.calArea();